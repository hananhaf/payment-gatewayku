node_modules
dist
.env
.env.local
*.log
.DS_Store
gateway.db
gateway.db-*

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Server bundle (built by build:server)
dist-server

# Real merchant config (secrets + QRIS) — never commit
merchants.json
